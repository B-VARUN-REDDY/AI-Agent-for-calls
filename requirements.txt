# Live voice pipeline + telephony
# Pinned to the stable 0.0.x API this bot targets (1.x reorganized modules).
pipecat-ai[anthropic,deepgram,cartesia,elevenlabs,silero]>=0.0.60,<1.0
twilio>=9.0.0
fastapi>=0.110
uvicorn>=0.29
requests>=2.31

# LLMs / transcription clients (also pulled by pipecat, pinned for the
# offline transcribe + analyze paths)
anthropic>=0.40
deepgram-sdk>=3.5

# Config
python-dotenv>=1.0

# Dev / tests (offline, no API keys needed)
pytest>=8.0
